from Hashcreate import *
from Computegraph import *
from Graphcreate import *

if __name__ == '__main__':
	# ls = {}
	# ls['name'] = 'nymphomania.n.04'
	# ls['pos'] = 'n'
	# ls['definition'] = 'Defect in females being addicted to sexual intercourse'
	# ls['lemma_names'] = ['nymphomaniac','randi']
	# ls['lemma_count'] = [50,10]
	# Newword_hash(ls, 'new')
	# Newword_compute('new')
	
	Newword_graph()
